[pytest]
markers =
    hil: Hardware-in-loop tests (run with SIYI_HIL=1)
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -ra --strict-markers -p no:launch_testing -p no:launch_testing_ros_pytest_entrypoint
